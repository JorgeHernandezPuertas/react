import { Button } from 'reactstrap'

export default function Formulario ({ datosUsuario }) {
  const campos = datosUsuario.map(() => {

  })

  return (
    <div className="formulario">

    </div>
  )
}
